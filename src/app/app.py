import streamlit as st
def boot_app():
    st.title("Resident Scheduling")

    

    #we want to cache the schedule so it doesn't re-run for on change events









if __name__ == "__main__":
   boot_app()
